name: CI

on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: "Set up Node.js"
        uses: actions/setup-node@v6
        with:
          node-version: "24"
      - name: "Install Dependencies"
        run: npm ci
      - name: "Build"
        run: npm run build
      - name: "Test"
        run: npm run test
      - name: "Check: STDIO server can start successfully"
        run: bash .github/scripts/check-server-starts.sh dev:stdio "MCP Server running on STDIO"
      - name: "Check: HTTP server can start successfully"
        run: bash .github/scripts/check-server-starts.sh dev:http "MCP Server running on"
